# SMTP settings (provide real SMTP credentials to send emails)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
FROM_EMAIL=Your Name <no-reply@example.com>

# Server
PORT=5000

# Other secrets
GROQ_API_KEY=
